{
	"private": true,
	"name": "applications",
	"version": "1.1.4",
	"type": "module",
	"author": "eccs0103",
	"packageManager": "npm@11.6.2",
	"engines": {
		"node": ">=22"
	},
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "npm run build && vite preview",
		"deploy": "vite build && wrangler deploy",
		"deploy:birthday-push": "wrangler deploy --config environment/workers/birthday-push/wrangler.jsonc",
		"typecheck": "tsc -p tsconfig.node.json && tsc -p tsconfig.web.json && tsc -p tsconfig.worker.json && tsc -p tsconfig.cloudflare.json",
		"test": "npm run build && vitest run",
		"extract:vocabulary": "tsx telegram-theme-converter/controllers/vocabulary-extractor.ts"
	},
	"devDependencies": {
		"@cloudflare/vite-plugin": "^1.45.0",
		"@types/node": "^24.10.1",
		"tsx": "^4.23.11",
		"typescript": "^6.0.3",
		"vite": "^7.3.0",
		"vitest": "^4.1.10",
		"wrangler": "^4.111.0"
	},
	"dependencies": {
		"adaptive-extender": "^1.0.6"
	},
	"overrides": {
		"undici": ">=7.28.0",
		"esbuild": "^0.28.1",
		"sharp": "^0.35.3"
	}
}
