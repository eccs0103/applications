"use strict";

import "adaptive-extender/web";

//#region Notification state
export enum NotificationState {
	idle = "idle",
	pending = "pending",
	ready = "ready",
	denied = "denied",
}
//#endregion
